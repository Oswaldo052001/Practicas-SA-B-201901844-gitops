{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cronjob-registro.serviceAccountName" . }}
  namespace: {{ .Values.global.namespace | default "sa-p5" }}
  labels:
    {{- include "cronjob-registro.labels" . | nindent 4 }}
{{- end }}
